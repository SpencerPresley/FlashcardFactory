What is the result of α(x + y) where α = 2 - x = [0 1 2]^T and y = [3 4 5]^T, [6 10 14]^T; What is the Euclidean norm of x = [0 1 2]^T, √5; What is the vector transpose of x = [0 1 2]^T, [0 1 2]; What is the result of the matrix-vector multiplication Ax where A = [[3 2 2] [1 3 1] [1 1 3]] and x = [0 1 2]^T, [6 5 7]^T; What is the result of the quadratic form x^⊤Ax where A = [[3 2 2] [1 3 1] [1 1 3]] and x = [0 1 2]^T, 19; What is the inner product of x = [0 1 2]^T and y = [3 4 5]^T, 14; What is the inner product between orthogonal vectors x = [0 1 2]^T and z = [1 2 -1]^T, 0; Is x^⊤y = Σ(xiyi) from i=1 to n generally true, True; Is x^⊤x = ∥x∥^2 generally true, True; Is x^⊤x = xx^⊤ generally true, False; Is (x −y)^⊤(y −x) = ∥x∥^2 −2x^⊤y + ∥y∥^2 generally true, False; Is AB = BA generally true for matrices, False; Is A(B + C) = AB + AC generally true for matrices, True; Is (AB)^⊤ = A^⊤B^⊤ generally true for matrices, False; Is x^⊤Ay = y^⊤A^⊤x generally true, True; Is A^⊤A = I if the columns of A are orthonormal generally true, True; What is a fair price (expected value) for a game where you win $15 if the difference between two 6-sided dice rolls is at least 3 and $0 otherwise, $5; If Pr(A- B) = 0 - Pr(A) = 0.4 - and Pr(A ∪ B) = 0.95 - what is Pr(B), 0.55; If A and B are independent with Pr(A) = 0.4 and Pr(A ∪ B) = 0.95 - what is Pr(B), 0.917; What is E[X] for X being the number of heads after 3 coin flips - where C1 has P(heads) = 0.3 and C2 has P(heads) = 0.9 - and each coin is chosen with equal probability, 1.8; What is Var[X] under the same conditions as the previous question, 0.99; What is E[Y] if Y = 1/(2+X) under the same conditions, 0.3472; Explain the variance paradox for i.i.d. random variables X1...Xn with variance σ^2, Var[X1+...+Xn] = nσ^2 due to independence - while Var[X+X] = 4σ^2 due to perfect correlation; What is the derivative of f(x) = 3x^2 − 2x + 5, 6x − 2; What is the derivative of f(x) = x(1 − x), 1 − 2x; What is the derivative of f(x) = x − log(p(x)) where p(x) = 1/(1 + exp(−x)), p(x); What is the gradient ∇f(x) of f(x) = x1^2 + exp(x2) where x = [x1 x2], [2x1 exp(x2)]; What is the gradient ∇f(x) of f(x) = exp(x1 + x2x3) where x = [x1 x2 x3], [exp(x1 + x2x3) x3*exp(x1 + x2x3) x2*exp(x1 + x2x3)]; What is the gradient ∇f(x) of f(x) = a^⊤x where x and a are in R^2, a; What is the gradient ∇f(x) of f(x) = x^⊤Ax where A = [[2 -1] [-1 1]] and x is in R^2, [4x1 − 2x2 -2x1 + 2x2]^T; What is the gradient ∇f(x) of f(x) = 1/2 * ∥x∥^2 where x is in R^d, x; What is the cost of running merge-sort on n numbers, O(n log n); What is the cost of finding the third-largest element in an unsorted list of n numbers, O(n); What is the cost of finding the smallest element greater than 0 in a sorted list of n numbers, O(log n); What is the cost of finding a key's value in a hash table with n numbers, O(1); What is the cost of computing Ax when A is n × d and x is d × 1, O(nd); What is the cost of computing x^⊤Ax when A is d × d and x is d × 1, O(d^2); What is the cost of computing AB when A is m × n and B is n × d, O(mnd); What function is used to display output on the screen in Python, print(); What operator is used for exponentiation in Python, **; What does integer division (//) return in Python, The quotient without decimals; What does the modulus operator (%) return in Python, The remainder of a division; Define an adjacency matrix for graph representation, A 2D array where A[i][j] = 1 if there's an edge from i to j; Define an adjacency list for graph representation, An array of linked lists where each list contains neighbors of vertex i; What data structure does Breadth-First Search (BFS) use, Queue; What is the time complexity of BFS and DFS, O(V+E); What is a topological sort, A linear ordering of vertices in a DAG such that for every edge (u-v) - u comes before v; What type of graph does topological sort work on, Directed Acyclic Graphs (DAGs); What is Android, A full software stack mobile operating system based on a Linux kernel; What is Dalvik, A specialized VM used in Android instead of JVM